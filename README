This is a set of simple utilities for working with SharePoint from a remote server, to implement apps.  For now,
this is largely a utility that leverages the EntityTokenManager in ServerIntegration to persist OAuth tokens.


DEPENDENCIES:

mammerla/ServerIntegration